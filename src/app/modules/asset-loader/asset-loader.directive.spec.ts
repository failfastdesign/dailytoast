import { AssetLoaderDirective } from './asset-loader.directive';

describe('AssetLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new AssetLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
